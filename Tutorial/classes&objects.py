from Phone import Phone
from inheriatance import expensive

galaxys10 = Phone("Samsung", "8gb", "128gb", 60)
iphone11 = Phone("Apple", "4gb", "64gb", 60)
pixel4 = Phone("Google", "6gb", "128gb", 90)

oneplus10pro =
print(pixel4.has_high_refreshrate())

# here, we added a function to check if a particular phone has a high refresh rate
# the function was added in the class file, proving that functions can be added to class files and accessed
# by the objects

